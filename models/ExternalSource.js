export class ExternalSource {
  constructor(url, description) {
      this.url = url; // URL to the external resource
      this.description = description; // Description or additional info about the resource
  }
}